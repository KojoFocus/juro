const Footer = () => {
  return (
    <footer className="w-full bg-gray-950 border-t border-gray-800 text-gray-400 text-sm py-6 px-6 text-center mt-20">
      <p>
        © {new Date().getFullYear()} JURO — Built with love, stories, and
        remixable chaos.
      </p>
    </footer>
  );
};

export default Footer;
